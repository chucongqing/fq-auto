server {
	listen 80;
	listen [::]:80;
	server_name $MYSITE;

	location /.well-known/acme-challenge {
		root /var/www/cert;
	}

	location / {
		rewrite	^/(.*)$ https://$host/$1 permanent;
	}
}
